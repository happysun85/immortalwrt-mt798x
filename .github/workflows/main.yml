name: Openwrt-cli

on:
  push:
    branches: 
      - openwrt-21.02
jobs:
  build_openwrt:
    name: build
    runs-on: ubuntu-latest
    steps:
      - name: make
        run: |
          git clone https://github.com/happysun85/immortalwrt-mt798x.git
         
